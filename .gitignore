# Dependencies
node_modules/

# ── Secrets / entorno · NUNCA commitear ──────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
id_ed25519
*_rsa
credentials*.json
secrets*.json
service-account*.json
*.secret

# Build / plataforma
.netlify/
.claude/

# Sistema / editor
.DS_Store
Thumbs.db

# ── Artefactos locales · NO van al repo público ──────────────────
# Docs internos con cifras de negocio / IDs de cuenta (auditorías, diagnósticos)
output/
# Creativos exportados (material de marketing, pesados)
ads/exports/
# Herramienta de exploración de paletas (dev, no enlazada)
explore-palettes.html
